1) pipeline setup



2) dependencies



3) instructions for running the
pipeline on a local machine using Docker Compose





clone the repo
navigate (via cmd) to the folder where the cloned directory is
run the service (docker-compose up)
check http://localhost:8080

---------------------------- General ------------------------------
1) about the connection with postgresql (via airflow)

2) apriori creation of db